{
  "name": "pips-life-workspace",
  "private": true,
  "scripts": {
    "build": "cd apps/backend && npm run build",
    "dev": "cd apps/backend && npm run dev",
    "start": "cd apps/backend && npm run start"
  },
  "dependencies": {
    "next": "^15.5.0"
  }
}
