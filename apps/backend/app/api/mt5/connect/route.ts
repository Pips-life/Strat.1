import { NextResponse } from 'next/server';
import { issueAccountSession, verifyAccountSession } from '@/lib/session';
const PROVISIONING_API=process.env.METAAPI_PROVISIONING_URL??'https://mt-provisioning-api-v1.agiliumtrade.agiliumtrade.ai';const METAAPI_TOKEN=process.env.METAAPI_TOKEN;const CLIENT_API=process.env.METAAPI_CLIENT_API_URL??'https://mt-client-api-v1.new-york.agiliumtrade.ai';
function errorMessage(body:string,fallback:string){try{const j=JSON.parse(body) as {message?:string;error?:string};return j.message||j.error||fallback}catch{return fallback}}
async function liveState(request:Request,accountId:string){verifyAccountSession(request,accountId);if(!METAAPI_TOKEN)throw new Error('METAAPI_TOKEN is not configured');const headers={accept:'application/json','auth-token':METAAPI_TOKEN};const pr=await fetch(`${PROVISIONING_API}/users/current/accounts/${encodeURIComponent(accountId)}`,{headers,cache:'no-store'});const pt=await pr.text();if(!pr.ok)throw new Error(`MetaApi account lookup failed: ${pt}`);const account=JSON.parse(pt) as {login?:number|string;server?:string;state?:string;connectionStatus?:string;region?:string;baseCurrency?:string;name?:string;manualTrades?:boolean};const base=account.region?`https://mt-client-api-v1.${account.region}.agiliumtrade.ai`:CLIENT_API;const read=async(path:string)=>{const r=await fetch(`${base.replace(/\/$/,'')}/users/current/accounts/${accountId}${path}`,{headers,cache:'no-store'});const t=await r.text();if(!r.ok)throw new Error(`MetaApi ${r.status}: ${t||'terminal state unavailable'}`);return t?JSON.parse(t):null};const [info,positions]=await Promise.all([read('/account-information?refreshTerminalState=true'),read('/positions?refreshTerminalState=true')]);return {accountId,account:{login:account.login??info?.login,server:account.server??info?.server,state:account.state,connectionStatus:account.connectionStatus,region:account.region,baseCurrency:account.baseCurrency??info?.currency,name:account.name??info?.name,manualTrades:account.manualTrades},accountInformation:info,positions:Array.isArray(positions)?positions:[]}}
export async function GET(request:Request){const accountId=new URL(request.url).searchParams.get('accountId')?.trim();if(!accountId)return NextResponse.json({error:'accountId is required'},{status:400});try{return NextResponse.json(await liveState(request,accountId),{headers:{'cache-control':'no-store'}})}catch(error){if(error instanceof Response)return error;return NextResponse.json({error:error instanceof Error?error.message:'MT5 status request failed'},{status:502})}}
export async function POST(request:Request){if(!METAAPI_TOKEN)return NextResponse.json({error:'METAAPI_TOKEN is not configured on the backend'},{status:503});if(!process.env.PIPSLIFE_SESSION_SECRET)return NextResponse.json({error:'PIPSLIFE_SESSION_SECRET is not configured on the backend'},{status:503});try{const body=await request.json() as {accountId?:string;login?:string;password?:string;server?:string;name?:string;broker?:string};if(body.accountId&&request.headers.get('x-pipslife-session'))return NextResponse.json(await liveState(request,body.accountId),{headers:{'cache-control':'no-store'}});const login=body.login?.trim(),password=body.password,server=body.server?.trim(),broker=body.broker?.trim();if(!login||!password||!server)return NextResponse.json({error:'login, password and server are required'},{status:400});if(!/^\d+$/.test(login))return NextResponse.json({error:'MT5 login must contain digits only'},{status:400});const headers={accept:'application/json','content-type':'application/json','auth-token':METAAPI_TOKEN};const existingResponse=await fetch(`${PROVISIONING_API}/users/current/accounts?query=${encodeURIComponent(login)}&limit=20`,{headers,cache:'no-store'});if(existingResponse.ok){const existing=await existingResponse.json() as Array<{_id?:string;login?:string|number;server?:string;state?:string;connectionStatus?:string}>;const match=existing.find(a=>String(a.login)===login&&a.server===server);if(match?._id)return NextResponse.json({accountId:match._id,state:match.state??'DEPLOYED',connectionStatus:match.connectionStatus??'UNKNOWN',server:match.server??server,sessionToken:issueAccountSession(match._id),reused:true})}const response=await fetch(`${PROVISIONING_API}/users/current/accounts`,{method:'POST',headers:{...headers,'transaction-id':crypto.randomUUID().replaceAll('-','')},body:JSON.stringify({login,password,server,name:body.name||`Pips-life MT5 ${login}`,platform:'mt5',magic:100001,manualTrades:false,type:'cloud-g2',keywords:broker?[broker]:undefined})});const responseBody=await response.text();if(!response.ok)return NextResponse.json({error:errorMessage(responseBody,'MetaApi account creation failed')},{status:response.status});const result=JSON.parse(responseBody) as {id?:string;state?:string};if(!result.id)return NextResponse.json({error:'MetaApi returned no account id'},{status:502});return NextResponse.json({accountId:result.id,state:result.state??'DEPLOYED',connectionStatus:'CONNECTING',server,sessionToken:issueAccountSession(result.id),reused:false},{status:response.status})}catch(error){if(error instanceof Response)return error;return NextResponse.json({error:error instanceof Error?error.message:'MT5 request failed'},{status:500})}}
