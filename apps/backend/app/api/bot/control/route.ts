import { NextResponse } from 'next/server';import { verifyAccountSession } from '@/lib/session';
const BOT_CONTROL_URL=process.env.PIPSLIFE_BOT_CONTROL_URL?.trim();const BOT_CONTROL_TOKEN=process.env.PIPSLIFE_BOT_CONTROL_TOKEN?.trim();
export async function GET(request:Request){const accountId=new URL(request.url).searchParams.get('accountId')?.trim();if(accountId){try{verifyAccountSession(request,accountId)}catch(e){if(e instanceof Response)return e;throw e}}if(!BOT_CONTROL_URL)return NextResponse.json({configured:false,state:'RUNNER_NOT_CONFIGURED',strategy:'001'});try{const r=await fetch(`${BOT_CONTROL_URL.replace(/\/$/,'')}/status`,{headers:BOT_CONTROL_TOKEN?{authorization:`Bearer ${BOT_CONTROL_TOKEN}`}:{},cache:'no-store'});const body=await r.text();return new NextResponse(body,{status:r.status,headers:{'content-type':r.headers.get('content-type')??'application/json','cache-control':'no-store'}})}catch(error){return NextResponse.json({configured:true,state:'RUNNER_UNREACHABLE',error:error instanceof Error?error.message:'runner unavailable'},{status:502})}}
export async function POST(request:Request){if(!BOT_CONTROL_URL)return NextResponse.json({configured:false,state:'RUNNER_NOT_CONFIGURED',error:'Strategy runner control endpoint is not configured'},{status:503});try{const p=await request.json() as {action?:'start'|'stop';strategy?:string;accountId?:string};if(!p.accountId)return NextResponse.json({error:'accountId is required'},{status:400});verifyAccountSession(request,p.accountId);if(p.action!=='start'&&p.action!=='stop')return NextResponse.json({error:'action must be start or stop'},{status:400});const r=await fetch(`${BOT_CONTROL_URL.replace(/\/$/,'')}/${p.action}`,{method:'POST',headers:{'content-type':'application/json',...(BOT_CONTROL_TOKEN?{authorization:`Bearer ${BOT_CONTROL_TOKEN}`}:{})},body:JSON.stringify({strategy:p.strategy??'001',accountId:p.accountId}),cache:'no-store'});const body=await r.text();return new NextResponse(body,{status:r.status,headers:{'content-type':r.headers.get('content-type')??'application/json','cache-control':'no-store'}})}catch(error){if(error instanceof Response)return error;return NextResponse.json({error:error instanceof Error?error.message:'runner command failed'},{status:502})}}
